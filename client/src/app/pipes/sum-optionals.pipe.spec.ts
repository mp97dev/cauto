import { SumOptionalsPipe } from './sum-optionals.pipe';

describe('SumOptionalsPipe', () => {
  it('create an instance', () => {
    const pipe = new SumOptionalsPipe();
    expect(pipe).toBeTruthy();
  });
});
